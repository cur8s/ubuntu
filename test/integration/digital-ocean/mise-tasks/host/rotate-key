#!/usr/bin/env sh
#MISE description="Re-key one baseline account via its sibling (set ROTATE_ACCOUNT + ROTATE_NEW_PUB_KEY)"
set -eu
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
ssh_task_preamble

: "${ROTATE_ACCOUNT:?set ROTATE_ACCOUNT=ansible|sysadmin}"
: "${ROTATE_NEW_PUB_KEY:?set ROTATE_NEW_PUB_KEY=/path/to/new.pub}"
droplet_playbook rotate_key "$@"
