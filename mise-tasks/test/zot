#!/usr/bin/env bash
#MISE description="Prove the zot example idempotent: two runs, the second changed=0"
set -euo pipefail
. "$MISE_CONFIG_ROOT/mise-tasks/test/example-suite.sh"

test_example zot qemu
