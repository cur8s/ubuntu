#!/usr/bin/env bash
#MISE description="Runs it twice; second pass must report changed=0"
#MISE depends=["example:link"]
set -euo pipefail
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
prepare_ansible_temp

test_example docker qemu
