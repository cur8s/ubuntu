#!/usr/bin/env sh
#MISE description="Additively add the baseline accounts to the scenario VM (next: qemu:play:converge)"
set -eu
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
ssh_task_preamble
qemu_keys_env

ADOPT_USER="${ADOPT_USER:-ubuntu}" qemu_ansible_playbook \
  "$MISE_CONFIG_ROOT/collection/playbooks/adopt.yml" \
  --private-key "$BOOTSTRAP_PUB_KEY"
