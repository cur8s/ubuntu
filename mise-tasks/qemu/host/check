#!/usr/bin/env sh
#MISE description="Is anything drifting? The converge report with nothing enforced"
set -eu
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
ssh_task_preamble

qemu_ansible_playbook "$MISE_CONFIG_ROOT/collection/playbooks/converge.yml" --check --diff
