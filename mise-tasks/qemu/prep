#!/usr/bin/env sh
#MISE description="One-time: throwaway lab keys + cloud image cache (next: qemu:up)"
#MISE depends=["qemu:keys", "qemu:fetch"]
#MISE quiet=true
set -eu
echo "QEMU lab prepared: lab keys generated, cloud image cached (no 1Password needed). Next: mise run qemu:up"
