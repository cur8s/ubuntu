#!/usr/bin/env bash
#MISE description="Close account doors (LOCK_ACCOUNTS, default ubuntu); run host:validate-reboot first"
#MISE confirm="Lock these accounts' doors on the QEMU VM?"
set -euo pipefail
. "$MISE_CONFIG_ROOT/collection/scripts/run-lab-playbook.sh"

LOCK_ACCOUNTS="${LOCK_ACCOUNTS:-ubuntu}" run_lab_playbook \
  "$MISE_CONFIG_ROOT/collection/playbooks/lock_accounts.yml"
