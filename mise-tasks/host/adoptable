#!/usr/bin/env bash
#MISE description="Can this host be adopted? A read-only verdict (next: host:adopt)"
set -euo pipefail
. "$MISE_CONFIG_ROOT/collection/scripts/run-lab-playbook.sh"
activate_test_credentials

ADOPT_USER="${ADOPT_USER:-ubuntu}" run_lab_playbook \
  "$MISE_CONFIG_ROOT/collection/playbooks/adoptable.yml" \
  --private-key "$BOOTSTRAP_PUB_KEY"
