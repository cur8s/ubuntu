#!/usr/bin/env bash
#MISE description="Upgrade every package; never reboots, reports if one becomes pending"
set -euo pipefail
. "$MISE_CONFIG_ROOT/collection/scripts/run-lab-playbook.sh"

run_lab_playbook "$MISE_CONFIG_ROOT/collection/playbooks/patch.yml"
