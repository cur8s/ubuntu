#!/usr/bin/env bash
#MISE description="Bring an existing host under the baseline — strictly additive, nothing is removed (next: host:converge)"
set -euo pipefail
. "$MISE_CONFIG_ROOT/collection/scripts/run-lab-playbook.sh"
activate_test_credentials

ADOPT_USER="${ADOPT_USER:-ubuntu}" run_lab_playbook \
  "$MISE_CONFIG_ROOT/collection/playbooks/adopt.yml" \
  --private-key "$BOOTSTRAP_PUB_KEY"
