#!/usr/bin/env bash
#MISE description="One-time: throwaway lab keys + cloud image cache (next: up)"
#MISE depends=["keys", "vm:fetch-image"]
#MISE quiet=true
set -euo pipefail
echo "QEMU lab prepared: lab keys generated, cloud image cached (no 1Password needed). Next: mise run up"
