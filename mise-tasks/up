#!/usr/bin/env bash
#MISE description="From zero: vm:build-conformant -> vm:start -> vm:wait-until-ready -> host:converge"
set -euo pipefail
mise run vm:build-conformant
mise run vm:start
mise run vm:wait-until-ready
mise run host:converge
echo "QEMU lab is up and converged."
