#!/usr/bin/env bash
#MISE description="Generate the lab's throwaway keypairs (no-op if present; no 1Password involved)"
#MISE hide=true
#MISE quiet=true
set -euo pipefail
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
export_lab_credentials
echo "QEMU lab keys ready in $QEMU_KEYS_DIR (ephemeral lab credentials — RFC-004)"
