#!/usr/bin/env sh
#MISE description="Close doors (default: root, the provider door); run do:play:validate-reboot first"
#MISE confirm="Lock these accounts' doors? For root, SSH stops working and recovery is the provider console."
set -eu
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
ssh_task_preamble

LOCK_ACCOUNTS="${LOCK_ACCOUNTS:-root}" ansible-playbook \
  -i "$(droplet_ip)," \
  "$MISE_CONFIG_ROOT/collection/playbooks/lock_accounts.yml"
