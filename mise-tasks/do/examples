#!/usr/bin/env sh
#MISE description="Run every example twice against the droplet; the second pass must report changed=0"
#MISE depends=["example:link"]
set -eu
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
ssh_task_preamble

example_run_all do
