#!/usr/bin/env sh
#MISE description="Operator-invoked full package update on the test VM (never reboots; reports if one is pending)"
set -eu
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
ssh_task_preamble

ansible-playbook \
  -i "$(droplet_ip)," \
  "$MISE_CONFIG_ROOT/collection/playbooks/update.yml"
