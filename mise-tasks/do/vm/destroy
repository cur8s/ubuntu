#!/usr/bin/env sh
#MISE description="Irreversible; asks for confirmation"
#MISE confirm="Destroy the DigitalOcean Ubuntu test VM? This cannot be undone."
set -eu
doctl compute droplet delete "$DROPLET_NAME" --force
