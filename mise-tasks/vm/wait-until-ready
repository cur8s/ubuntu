#!/usr/bin/env bash
#MISE description="Rides out the first boot (next: host:converge)"
set -euo pipefail
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
export_lab_credentials
# The lab's private keys sit beside their .pub files, so the harness can
# probe with the file directly — no agent involved.
qvm wait-until-ready ansible "$QEMU_KEYS_DIR/ubuntu-ansible"
