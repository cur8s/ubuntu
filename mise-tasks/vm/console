#!/usr/bin/env sh
#MISE description="Follow it live — the debug window when SSH is down"
set -eu
if [ ! -f "$QVM_DIR/console.log" ]; then
  echo "No console log yet (mise run up creates and boots the VM)." >&2
  exit 1
fi
exec tail -n 100 -F "$QVM_DIR/console.log"
