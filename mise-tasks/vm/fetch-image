#!/usr/bin/env bash
#MISE description="Download and checksum-verify the cloud image into the cache (no-op if cached)"
#MISE hide=true
set -euo pipefail
. "$MISE_CONFIG_ROOT/mise-tasks/lib.sh"
qvm fetch-image
