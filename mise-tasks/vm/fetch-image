#!/usr/bin/env bash
#MISE description="Optional pre-warm: the ~600MB cloud image, checksum-verified (no-op if cached; up fetches on demand)"
set -euo pipefail
bash "$MISE_CONFIG_ROOT/mise-tasks/vendor/qemu-vm.sh" fetch-image
echo "Image ready in $QVM_CACHE_DIR."
