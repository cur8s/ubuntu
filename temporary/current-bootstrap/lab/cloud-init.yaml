#cloud-config
disable_root: false
ssh_pwauth: false
package_update: false

write_files:
  - path: /etc/motd
    owner: root:root
    permissions: '0644'
    content: |
      cur8s Ubuntu bootstrap lab host

      This VM is intended for disposable testing.
