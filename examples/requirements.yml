---
# Install baseline.ubuntu from its git repository (RFC-008: Release and
# Versioning). main is the prod release; once release tags exist, pin one
# (e.g. version: v24.4.0) for reproducible installs and rollback.
collections:
  - name: https://github.com/cur8s/ubuntu.git
    type: git
    version: main
