---
# Minimal consumer playbook: enforce the baseline, then layer your own
# plays after it. Every tier enforces the baseline (RFC-001), so this
# import comes first, always.
- name: Converge the baseline
  ansible.builtin.import_playbook: cur8s.ubuntu.converge

# Your workload plays follow. They connect through the baseline's automation
# account (RFC-011: Conventions Contract).
#
# - name: Apply the workload
#   hosts: all
#   remote_user: ansible
#   become: true
#   roles:
#     - my_namespace.my_collection.my_role
